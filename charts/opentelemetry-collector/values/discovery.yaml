# Copyright 2026 BitWise Media Group Ltd
# SPDX-License-Identifier: MIT
#
# Discovery-only values — LOAD-BEARING here: the chart refuses to render
# without mode, and the runtime (flux-manifests) deploys the -contrib image
# (the googlecloud / googlemanagedprometheus exporters only exist there), so
# discovery must mirror that exact image.
#
# The tag below is FACT, not intent: it is written by the .images.track rule
# in manifest.yaml (newest soaked contrib 0.x tag) and re-picked by
# `patchy mirror upgrade` — never hand-edit it. Without the pin the chart
# falls back to its appVersion, which lags contrib image releases.
mode: deployment
image:
  repository: ghcr.io/open-telemetry/opentelemetry-collector-releases/opentelemetry-collector-contrib
  tag: 0.158.0
